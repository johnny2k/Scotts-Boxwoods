<%= form_for [:admin, @product] do |f| %>
	<% if @product.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved.</h2>
    
			<ul>
				<% @product.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div>
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>

	<div>
		<%= f.label :description %><br />
		<%= f.text_area :description %>
	</div>

	<div>
	 <%= f.label :image %><br />
	 <%= f.text_field :image %>
	</div>

	<div>
	 <%= f.label :price %><br />
	 <%= f.text_field :price %>
	</div>

	<div>
		<%= f.label :stock %><br />
		<%= f.text_field :stock %>
	</div>

	<div>
		<%= f.label :category %></br>
		<%= f.text_field :category %></br>
	</div>

	<div>
		<%= f.submit "Save changes", :style => 'color:white', :class => 'cool_button' %>
	</div>

<% end %>
